# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (never commit)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# vercel
.vercel

# Claude Code state
.claude/

# typescript
*.tsbuildinfo

# scraped data dumps - keep enriched (deployed); raw is regeneratable
/data/scraped/tiktok-*.json
/data/scraped/instagram-*.json
!/data/scraped/.gitkeep
!/data/scraped/enriched-*.json

# probe verification outputs (debugging)
/data/probes/

# uploaded videos (demo only)
/public/uploads

# local test fixtures (PDF / brief samples)
/scripts/*.pdf
/scripts/sample-*

.vercel
.env*.local

# P1 enrichment artifacts (large mp4s + transient error logs)
data/raw-mp4s/
data/enrichment-errors.json
data/rescrape-*.json

# Multi-window worktree isolation (superpowers:using-git-worktrees)
.worktrees/
